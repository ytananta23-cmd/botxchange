# VITE_API_BASE_URL: Backend REST API base URL.
# The WebSocket connection (src/lib/ws.ts) is derived automatically from
# this same host, so you only need to set this one value.
VITE_API_BASE_URL="https://your-backend.onrender.com/api"
